name: circular_check_box
description: Same as the regular checkbox, but circular!
version: 1.0.3
author: aarol
homepage: 
repository: https://github.com/aarol/circular_check_box.git

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
